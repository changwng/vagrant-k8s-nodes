apiVersion: v1
kind: Service
metadata:
  name: pgadmin-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: pgadmin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32083
