apiVersion: v1
kind: Service
metadata:
  name: backstage-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: backstage
  ports:
    - protocol: TCP
      port: 7000
      targetPort: 7000
      nodePort: 32003
