apiVersion: v1
kind: Service
metadata:
  name: harbor-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: harbor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32002
